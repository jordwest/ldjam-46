{
  "scripts": {
    "start": "parcel src/index.html",
    "build": "rm -rf dist && parcel build --public-url '.' src/index.html && cd dist && zip -r ../phobos.zip . -i *"
  },
  "dependencies": {
    "@types/howler": "^2.1.2",
    "howler": "^2.1.3",
    "parcel-bundler": "^1.12.4",
    "twgl.js": "^4.14.2",
    "typescript": "^3.8.3"
  },
  "devDependencies": {
    "glslify-bundle": "^5.1.1",
    "glslify-deps": "^1.3.1"
  }
}
